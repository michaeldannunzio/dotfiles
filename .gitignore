# Repository-wide safety net. Each layer also carries its own .gitignore.

# Never commit private keys or secrets
*.pem
*.key
id_*
!id_*.pub
.ssh/

# Nix build output and direnv state
result
result-*
.direnv/
*.hm-bak

# Local machine output
baseline/
*.bak
*.log

# OS cruft
.DS_Store
*.swp
